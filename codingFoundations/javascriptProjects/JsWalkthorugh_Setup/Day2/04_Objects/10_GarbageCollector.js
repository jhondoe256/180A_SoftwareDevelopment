//Garbage Collection

// Automatic de-allocation of memory 
// so, if the app is running and something is not being used then the 'GargabeCollector' will
// remove the memory.

let obj={};
console.log(obj);
//...later on obj isn't being used to it'll get deleted.