// const  -> we can assign a value, but once assigned it cannot be changed.

const playerSpeed = 3.53;
console.log(playerSpeed);

//! playerSpeed=77;
//! console.log(playerSpeed);  alt+ left_mouse_button  (hit esc to exit)

let superSpeed = playerSpeed *3;
console.log(superSpeed);


